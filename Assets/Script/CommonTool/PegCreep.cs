using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PegCreep : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("boxgroup")]    public List<GameObject> Railroad;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
